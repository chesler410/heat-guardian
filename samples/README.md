# samples/

Drop real example heat sheets / psych sheets here for development.

**These are git-ignored** (see root `.gitignore`) because they contain minors' names and
times. Do not commit real meet PDFs. If we need a committed fixture for tests, use a
**redacted/synthetic** sheet and name it `*.fixture.pdf`.
